# Deployment repair, 26 September 2026

Reproduced all six reported failures on `dc2aedb`, which contains canonical `b9bcff8`. The Quick Fight dependency closure checker, its three entry points, and the three hen modules remain intact.

- **Factory fixture:** `1073170` intentionally added 16 flat and 20 sloped roof modules over the two single-story factory buildings and marked the two original demonstration roofs climbable. The deployed fixture has 434 props, including 38 climbable roofs. Updated `Factory-test.json` to match, with explicit roof positions and kinds in the test. Guard, boundary, cargo, malformed-map and exact fixture-agreement checks remain.
- **Cliff rotation:** full tiles can rotate unchanged; shaped masks rotate clockwise. Tests now cover all four orientations and exact restoration, plus atomic rejection beside an incompatible neighbor. Invalid masks, occupied starts and the cliff limit still reject.
- **Ramp banks:** these meshes deliberately use physical coordinates, unlike normalized primitive meshes: x/z span [-0.5, 0.5], height spans [0, 2]. Every segment's outside edge meets the 2 m cliff; its inside edge follows its portion of the ramp. Tests assert these explicit bounds, finite normals/positions, and nonempty triangles. Existing seam and ray tests remain unchanged.
- **Hen clearance:** the collision was also reproducible at the final northwest lookout position, not only in transit. The light at local (-1.35, -1.10) overlapped that occupied slot. Moved its mount toward the north deck edge to (-1.35, -1.90), still opposite the south ladder and inside the platform. Rendering and illumination share `WOODEN_LIGHT_MOUNT`. No hen anatomy, pose, route, contacts, endpoint, cleanup behavior or clearance tolerance was changed. The visible-surface test now checks the rotated tower too. Rail-shadow sample locations follow the new mount, with both visible and tactical obstruction still required and a clear sample between rails.
- **Hybrid catalog:** ramps and banks already have dedicated rendering paths. The catalog test exercises those paths explicitly, verifies geometry registrations and finite output, and confirms their broad-phase boxes do not become opaque rectangular blockers. Slope and bank ray tests cover their actual occlusion.
- **Base packaging:** added the missing dependencies to the base Pages manifest; its transitive manifest check stays enabled independently of the outer 3D build.
- **Camera hint:** startup/Center and Overview now retain WASD/arrow instructions. The browser check asserts the active hint after initial load, Center, Overview and returning from Overview.
- **Asset audit exposed after the six fixes:** all 962 tests passed, then `check-assets.mjs` rejected procedural catalog entries without individual PNGs. It now explicitly verifies registered ramp/bank/roof geometry, roof backing assets, and diagonal-road procedural material registrations/pixel data. The exact PNG manifest comparison remains for raster assets, and existing PNG dimensions, channels, references and orphan checks still run.

Browser checks accept `TACTICS_BASE_URL`, allowing the same checks against the built package and public Pages URLs. `check-deployed-editor-playtest.mjs` additionally checks save, export/reopen and the real editor-to-game popup handoff. Build sequentially: deployment tests and build scripts share `.pages-output`.

Validation: full npm run check passed (962 tests, zero failures, plus the asset audit); core sync and final 3D build passed. Built-package browser checks passed for title-to-Quick-Fight navigation, camera/help, copied variants, workbench/roofs, wall ramps, save/export/reopen and live editor playtest. Hen clearance was reviewed visually at the walk-away and final standing positions.
