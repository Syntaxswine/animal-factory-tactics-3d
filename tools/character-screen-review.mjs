import {createRequire} from 'node:module';import fs from 'node:fs';import assert from 'node:assert/strict';
const {chromium}=createRequire(import.meta.url)(process.env.PLAYWRIGHT_PATH||'playwright'),browser=await chromium.launch({channel:'msedge',headless:true});
try{const page=await browser.newPage({viewport:{width:1440,height:1000}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4323/tactics/battle-3d.html');await page.waitForFunction(()=>window.battle3d?.state,{},{timeout:120000});await page.click('#character');assert.equal(await page.locator('.stat-row').count(),13);assert.equal(await page.evaluate(()=>battle3d.paused),true);await page.waitForFunction(()=>[...document.querySelectorAll('#character-screen img')].every(i=>i.complete&&i.naturalWidth));const time=await page.evaluate(()=>battle3d.clock.minutes);await page.waitForTimeout(350);assert.equal(await page.evaluate(()=>battle3d.clock.minutes),time);await page.locator('.dossier-roster button').nth(1).click();assert.equal(await page.locator('#character-name').textContent(),await page.locator('.dossier-roster button').nth(1).innerText());fs.mkdirSync('artifacts/battle-3d/character-screen',{recursive:true});await page.screenshot({path:'artifacts/battle-3d/character-screen/desktop.png'});await page.keyboard.press('Escape');assert.equal(await page.evaluate(()=>battle3d.paused),false);assert.equal(await page.evaluate(()=>document.activeElement.id),'character');await page.click('#pause');await page.click('#character');await page.click('#character-close');assert.equal(await page.evaluate(()=>battle3d.paused),true);await page.setViewportSize({width:390,height:844});await page.click('#character');await page.screenshot({path:'artifacts/battle-3d/character-screen/mobile.png'});assert.equal(await page.evaluate(()=>{const d=document.querySelector('#character-screen');return d.scrollWidth<=d.clientWidth;}),true);assert.deepEqual(errors,[]);console.log(JSON.stringify({stats:13,sprites:true,roster:true,pauseRestored:true,mobile:true,errors}));}finally{await browser.close();}

